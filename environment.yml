# environment.yml
name: multi-lang-book
channels:
  - conda-forge
  - default
dependencies:
  - python         # Python interpreter
  - octave                # GNU Octave executable
  - jupyter-book
  - pip:
    - ipykernel           # Python Kernel
    - octave_kernel       # Octave Kernel