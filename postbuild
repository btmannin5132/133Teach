#!/bin/bash

# 1. Explicitly load the graphics package
# 2. Force Octave to use the 'gnuplot' backend for plots
octave --no-gui --eval 'pkg load graphics; graphics_toolkit ("gnuplot");'

# Ensure the system-wide Octave path is correctly saved (optional but safe)
octave --no-gui --eval 'savepath'